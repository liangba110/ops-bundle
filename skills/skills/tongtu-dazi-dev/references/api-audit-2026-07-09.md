# API Audit: 2026-07-09

Full cross-reference of frontend `api.*()` calls vs backend Flask blueprint routes.

## ❌ BROKEN — Frontend Calls Without Backend Route

| # | File | Line | Frontend Call | Fix |
|---|------|------|---------------|-----|
| 1 | `PlaymateHome.vue` | 155 | `api.post('/order/accept/${id}')` → 404 | `api.put('/playmate/accept-order/${id}')` |
| 2 | `PlaymateHome.vue` | 167 | `api.post('/order/reject/${id}')` → 404 | `api.put('/playmate/reject-order/${id}')` |

Both `order_bp` routes don't exist. Correct routes are in `playmate_bp` with PUT method.

## ⚠️ UNUSED — Backend Routes Never Called

### Entire Blueprint Unused

| Blueprint | Prefix | Routes | Reason |
|-----------|--------|--------|--------|
| `platform_review_v2` | `/api/review/v2` | 10 routes (verify/upload-id, verify/submit, verify/status, rank/submit, report, verify/list, verify/approve, verify/reject, reports, report/handle) | Identity verification uses `user_bp` (/upload-id, /verify) + `admin_bp` (/verifies, /verify/*/approve, /verify/*/reject) |
| `payment_bp` | `/api/payment` | `/pay` (POST) | Frontend calls `/order/pay` (order_bp) instead |

### Partial Routes Unused

| Blueprint | Unused Routes |
|-----------|---------------|
| `review_bp` (v1) | `/submit`, `/reply`, `/useful`, `/complaint` — only `/my` and `/list` used |
| `admin_bp` | `/register`, `/reviews`, `/security-alert`, `/recent-anomalies`, `/ban-ip` |
| `attendance_bp` | `/batch-checkin` |
| `chat_bp` | `/unread` |
| `cs_bp` | `/unread-count`, `/admin/unread-total` |
| `config_bp` | `/list` |
| `companion_bp` | `/photos/delete` |
| `wallet_bp` | `/income/detail` (newly added, no consumer yet) |
| `main.py` | `/api/backups/latest` |

### Blueprint With No Routes

| Blueprint | File | Note |
|-----------|------|------|
| `audit_log_bp` (`/api/audit`) | `audit_log.py` | Registered in main.py but has zero @route decorators — pure utility module |

## ✅ MATCHED — All 114 Correctly Connected Calls

Verified across 23 blueprint modules + main.py routes. Full report: `/opt/ttdazi/hermes/api-audit-report.md`

## Stats

| Category | Count |
|----------|-------|
| Broken (needs fix) | 2 |
| Unused backend routes | 24 |
| Matched | 114 |
| Total backend routes | 138 |
