# 同途搭子 API 审计报告模板

> 审计日期：{{DATE}}
> 审计范围：后端全部 Blueprint 路由 + main.py 直联路由 ↔ 前端 53 个 .vue 页面

## 审计发现问题

### ❌ 缺失 API（前端调了后端无）

| # | 前端页面 | 行号 | 调用 | 后端预期路径 | 严重程度 |
|---|----------|------|------|-------------|---------|
| 1 | Orders.vue | 185 | `api.post('/order/cancel')` | `POST /api/order/cancel` | 🔴 高 |

### ⚠️ HTTP Method 不匹配

| # | 前端页面 | 行号 | 前端方法 | 后端需要 | 路径 |
|---|----------|------|---------|---------|------|
| 1 | PlaymateHome.vue | 143 | POST | PUT | `/api/playmate/status` |

### 🟡 REST 风格不匹配

| # | 页面 | 前端路径 | 后端路径 | 差异 |
|---|------|---------|---------|------|
| 1 | AdminContent.vue | `/admin/banner/create` POST | `/api/admin/banners` POST | 路径 + 方法均不同 |

### 🚫 Vant 残留

| # | 文件 | 行号 | 遗留代码 |
|---|------|------|---------|
| — | ✅ 无 | — | 所有页面已迁移 safeToast |

## 后端未使用路由统计

| 类别 | 数量 | 说明 |
|------|------|------|
| 管理端未使用 | N | 审核操作、安全告警等 |
| 用户端未使用 | N | 陪玩师流程、旧版评价等 |
| 其他 | N | main.py 直联路由 |

## 建议修复优先级

1. 🔴 缺失 API — 新增后端端点
2. 🟡 方法不匹配 + REST 风格 — 对齐前后端
3. 🟡 陪玩师流程 — 补全 PlaymateOrders.vue 的接单/完成/拒绝操作
